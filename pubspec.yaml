name: insured
description: "Get insured in minutes. Manage policies, pay premiums, and issue certificates – all from one dashboard."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.11.4

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter 

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  model_viewer_plus: ^1.10.0
  flutter_animate: ^4.5.2
  google_fonts: ^8.0.2
  flutter_riverpod: ^3.2.1
  go_router: ^17.1.0
  shared_preferences: ^2.5.4
  image_picker: ^1.2.1
  # FOr ML
  # google_mlkit_text_recognition: ^0.15.1
  # google_ml_kit: ^0.21.0
  permission_handler: ^12.0.1
  font_awesome_flutter: ^11.0.0
  intl: ^0.20.2
  file_picker: ^10.3.10
  flutter_localizations:
    sdk: flutter
  flutter_gen: ^5.13.0+1
  local_auth: ^3.0.1
  # flutter_biometric_auth_plus: ^0.0.2
  connectivity_plus: ^7.0.0
  geocoding: ^4.0.0
  geolocator: ^14.0.2
  dropdown_button2: ^3.0.0
  flutter_staggered_grid_view: ^0.7.0
  url_launcher: ^6.3.2
  # LEMEE SEE IF WILL BE AUTO ADDED:::::::::::::::
  # flutter_riverpod: ^2.4.9       # state management
  # go_router: ^13.0.1              # navigation
  # # google_fonts: ^6.2.1            # typography
  # # flutter_animate: ^4.5.0         # smooth animations
  # path_provider: ^2.1.1           # access documents directory
  # shared_preferences: ^2.2.2      # simple storage

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  generate: true
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    # - images/a_dot_burr.jpeg
    # - images/a_dot_ham.jpeg
    - assets/data/data.json
    
    - assets/models/padlock_closed.glb
    - assets/models/padlock_opened.glb

    - assets/3d/padlock_closed.glb
    - assets/3d/padlock_opened.glb 
    - assets/3d/Astronaut.glb    
    - assets/3d/monstera_plant.glb    
    - assets/images/logos/logo.png
    - assets/images/logos/logo_black.png
    - assets/images/logos/inscloud.png

    # insurer_assets
    - assets/images/insurer_assets/geminia_insurance.png
    - assets/images/insurer_assets/apa_insurance.png  
    - assets/images/insurer_assets/sanlam_insurance.png
    - assets/images/insurer_assets/jubilee_allianz_insurance.png
    - assets/images/insurer_assets/fidelity_insurance.png
    - assets/images/insurer_assets/prudential_life_insurance.png
    - assets/images/insurer_assets/prudential_life_insurance.png
    - assets/images/insurer_assets/monarch.png
    - assets/images/insurer_assets/default_insurance.png


    - assets/images/insurer_assets/first_assurance.png
    - assets/images/insurer_assets/britam.png
    - assets/images/insurer_assets/madison.png
    - assets/images/insurer_assets/kenya_orient.png
    - assets/images/insurer_assets/old_mutual.png
    - assets/images/insurer_assets/heritage.png
    - assets/images/insurer_assets/cic.png
    - assets/images/insurer_assets/cannon.png
    - assets/images/insurer_assets/pioneer.png
    - assets/images/insurer_assets/star.png
    - assets/images/insurer_assets/occidental.png
    - assets/images/insurer_assets/direct.png
    
    - assets/images/insurer_assets/ga_insurance.png
    - assets/images/insurer_assets/icea_insurance.png
    - assets/images/insurer_assets/pacis_insurance.png
    - assets/images/insurer_assets/ncba_insurance.png
  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package


